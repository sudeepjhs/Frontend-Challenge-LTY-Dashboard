import { Gender } from "../chart";

// Represents gender data with statistics or values related to each gender category.
export const DummyGenderData: Gender = {
  male: [14, 10, 7, 4],
  female: [22, 16, 12, 5],
  others: [6, 5, 3, 1],
};

/* The code `export const DummyEngagementData = [35, 20, 65, 50, 95, 55, 80];` is exporting a constant
named `DummyEngagementData` that is an array of numbers. This array likely represents some kind of
engagement data, with each number possibly corresponding to different engagement metrics or values.
By exporting this constant, it can be imported and used in other parts of the codebase. */
export const DummyEngagementData = [35, 20, 65, 50, 95, 55, 80];
